{
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "dev": "concurrently \"npm run start:dev --prefix back-end\" \"npm run dev --prefix front-end\"",
    "install:all": "npm install --prefix back-end && npm install --prefix front-end",
    "build:all": "npm run build --prefix front-end && npm run build --prefix back-end",
    "start:prod": "node back-end/dist/main"
  }
}
